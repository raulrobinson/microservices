server:
  port: 8081
  application:
    name: config-service
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/raulrobinson/microservices
          searchPaths: config-data
          username: raulrobinson
          password: R7k4fevb
          default-label: zipkin-sleuth
  security:
    user:
      name: root
      password: s3cr3t
